// EN-first: this locale renders the English Agents copy until the
// translation pass (/extract then /translate) writes a real de version.
// See marketing/web/ASSUMPTIONS.md A-37.
import en from './en';
export default en;
