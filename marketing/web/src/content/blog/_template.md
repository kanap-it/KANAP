---
# Copy this file to  en/<slug>.md  (and  fr/<slug>.md  for the French version).
# The filename is the URL:  en/my-article.md  →  kanap.net/blog/my-article
# Files starting with "_" are ignored, so this template is never published.
#
# Full instructions: web/BLOG.md

title: "Article title in sentence case"
description: "One or two sentences. Shown on the index, in search results and in the RSS feed."
date: 2026-01-31            # YYYY-MM-DD. Future dates are not built.
topic: agents               # agents | self-hosting | cost | glpi | product
author: "Your name"
authorRole: "Founder, CIO"  # optional
draft: true                 # switch to false to publish
# updated: 2026-02-15       # optional revision date
# translationKey: my-article  # only if the FR and EN filenames differ
# ogImage: /og/blog/my-article.png  # optional, generated by `npm run og -- blog`
---

Opening paragraph. Say in two or three sentences what the reader will get.
The first paragraph is not repeated from `description`, so write it fresh.

## A section heading

Body text. Plain Markdown: **emphasis**, [links](https://kanap.net/features/agents),
`inline code`, lists.

- One point per line
- Keep lists short

> A pull quote that stands on its own line.

![A proposal as the technician sees it: the draft, the sources and the decision](/screenshots/agent-proposal.png)

```bash
# Code blocks keep their language for highlighting
docker compose up -d
```

<figure class="stat">
  <b>4 in 5</b>
  <span>drafts approved without edits by the end of week four.</span>
</figure>

Closing paragraph. End on the point, not on a summary.
