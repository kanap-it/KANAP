---
import TrialError from '../../components/trial/TrialError.astro';
import { getLocaleFromUrl } from '../../i18n/ui';
import { getTrialContent } from '../../content/resolve';
const copy = getTrialContent(getLocaleFromUrl(Astro.url)).expired;
---
<TrialError copy={copy} />
