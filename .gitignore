# macOS
.DS_Store

# Node.js dependencies
node_modules/

# Build outputs
dist/
backend/dist/
frontend/dist/
coverage/

# TypeScript
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE/editor
.vscode/
.idea/
.vs/

# Env files (keep examples)
**/.env
**/.env.*
!**/.env.example
!**/.env.*.example
backend/env.dev

# Docker (local build caches)
.docker/

CLAUDE.md
infra/docker-compose.yml
.obsidian

# Internal files (not for public repo)
INDEXES-FIXED.md
PERFORMANCE-INDEXES-CHECKLIST.md
SECURITY-AUDIT-REPORT.md
SECURITY-REVIEW-2026-07-05.md
TROUBLESHOOT-QA.md
TROUBLESHOOT-QA-LOCAL-PG.md
share.html
/package-lock.json
planning/
steps/
doc/.obsidian/
doc/archive/
doc/operations/
doc/planning/
doc/templates/
doc/cheatsheets/__pycache__/
# MkDocs build output. Cloudflare Pages rebuilds this from source on every push
# to main (see doc/help/DEPLOYMENT.md), so the committed copy served no purpose
# beyond going stale and swamping every docs diff.
doc/help/site/
# AI tooling
.claude/
.playwright-mcp/
.codex/*
!.codex/skills/
.codex/skills/*
!.codex/skills/kanap-design-system/
!.codex/skills/kanap-design-system/**
AGENTS.md
# Translation state. The docs manifest is what detects "English changed, so
# fr/de/es are now stale", so it has to be shared — the translate-docs skill
# already documents it as committed. The app-locale manifest stays local.
# Note: the glob must be /translation-state/* rather than translation-state/,
# because git cannot re-include a file whose parent directory is excluded. That
# anchors it to the repo root, so nested copies need their own rule.
**/translation-state/
!/translation-state/
/translation-state/*
!/translation-state/docs-source-hashes.json
/tasks/
